import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function EventsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Events & Meetups</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Browse and create local pet events. Coming soon!</p>
      </CardContent>
    </Card>
  );
}
